<h1>Listing contacts</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Apellido</th>
    <th>Email</th>
    <th>Teléfono</th>
    <th>Asunto</th>
    <th>Mensaje</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.nombre %></td>
    <td><%= contact.apellido %></td>
    <td><%= contact.email %></td>
    <td><%= contact.telefono %></td>
    <td><%= contact.asunto %></td>
    <td><%= contact.mensaje %></td>
    <td><%= link_to 'Ver', contact %></td>
    <td><%= link_to 'Editar', edit_contact_path(contact) %></td>
    <td><%= link_to 'Eliminar', contact, confirm: 'Esta seguro de eliminar?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />
<div class="botontabla">
<%= link_to 'Nuevo Contacto', new_contact_path %>
</div>